name: Publish Sanitized Release

on:
  push:
    paths:
      - eecs3404-release-20260910.zip
      - .github/workflows/publish-release.yml

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Extract verified release
        run: |
          unzip -o eecs3404-release-20260910.zip
          rm eecs3404-release-20260910.zip

      - name: Commit release contents
        run: |
          git config user.name "Erfan Razmand"
          git config user.email "114947956+mikeyoknow@users.noreply.github.com"
          git add -A
          git commit -m "Publish sanitized joint portfolio release"
          git push
